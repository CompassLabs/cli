// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package earn

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var balancesCmdMeta = []flagutil.FlagMeta{
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"arbitrum", "base", "ethereum"}, Description: "options: arbitrum, base, ethereum [required]"},
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The address of the owner of the earn account. [required]"},
}

// initBalancesCmd initializes the balances command.
func initBalancesCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "balances",
		Short:   "Get token balances",
		Long:    "Get token balances and transfer history for an earn account.\n\nReturns on-chain token balances for all tokens the earn account has interacted with,\nalong with the complete transfer history for each token. Balances are keyed by token\nsymbol for easy access.\n\nUse this endpoint to display account balances, track token movements, or build\ntransaction history interfaces.",
		Example: "  compass earn balances --chain base --owner 0x01E62835dd7F52173546A325294762143eE4a882",
		RunE:    runBalancesCmd,
	}
	flagutil.RegisterFlags(cmd, balancesCmdMeta)
	if err := flagutil.ValidateMeta[operations.V2EarnBalancesRequest](balancesCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for balances: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runBalancesCmd executes the balances command.
func runBalancesCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, balancesCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, balancesCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.V2EarnBalancesRequest](cmd, balancesCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Earn.Balances(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
