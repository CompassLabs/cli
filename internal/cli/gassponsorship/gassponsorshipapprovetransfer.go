// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package gassponsorship

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var gasSponsorshipApproveTransferCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The wallet address that owns the Earn Account. [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"base", "ethereum", "arbitrum", "hyperevm"}, Description: "The chain to use. (options: base, ethereum, arbitrum, hyperevm) [required]"},
	{FlagName: "token", Shorthand: "t", FieldPath: "Token", Kind: flagutil.FlagKindString, Required: true, Description: "The token you would like to transfer with gas sponsorship. [required]"},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "Optionally request gas sponsorship. If set to `true`, EIP-712 signature data will be returned that must be signed by the `owner` and submitted to the `/gas_sponsorship/prepare` endpoint."},
}

// initGasSponsorshipApproveTransferCmd initializes the gas-sponsorship-approve-transfer command.
func initGasSponsorshipApproveTransferCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "gas-sponsorship-approve-transfer",
		Short:   "Approve token transfer",
		Long:    "Set up a one-time Permit2 allowance for gas-sponsored token transfers.\n\nRequired when using [/earn/transfer](https://docs.compasslabs.ai/v2/api-reference/earn/transfer-tokens-tofrom-account) or [/credit/transfer](https://docs.compasslabs.ai/v2/api-reference/credit/transfer-tokens-tofrom-account) with `gas_sponsorship=true`. This allowance only needs to be set up once per token.\n\n**With gas sponsorship (`gas_sponsorship=true`):**\n- Returns EIP-712 typed data for the owner to sign off-chain\n- Submit signature + typed data to [/prepare](https://docs.compasslabs.ai/v2/api-reference/gas-sponsorship/prepare-gas-sponsored-transaction)\n- Only works for tokens that support EIP-2612 permit (e.g., USDC)\n\nSome tokens, like USDT and WETH, do not support EIP-2612 permit. For these tokens, set `gas_sponsorship=false` to receive an unsigned transaction that the owner must sign, submit, and pay gas for.",
		Example: "  compass gas-sponsorship gas-sponsorship-approve-transfer --owner 0x01E62835dd7F52173546A325294762143eE4a882 --chain base --token USDT",
		RunE:    runGasSponsorshipApproveTransferCmd,
		Aliases: []string{"gsat"},
	}
	flagutil.RegisterFlags(cmd, gasSponsorshipApproveTransferCmdMeta)
	if err := flagutil.ValidateMeta[components.ApproveTransferRequest](gasSponsorshipApproveTransferCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for gas-sponsorship-approve-transfer: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runGasSponsorshipApproveTransferCmd executes the gas-sponsorship-approve-transfer command.
func runGasSponsorshipApproveTransferCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, gasSponsorshipApproveTransferCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, gasSponsorshipApproveTransferCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.ApproveTransferRequest](cmd, gasSponsorshipApproveTransferCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.GasSponsorship.GasSponsorshipApproveTransfer(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
