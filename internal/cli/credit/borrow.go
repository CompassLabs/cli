// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package credit

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var borrowCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The address that owns the Credit Account. [required]"},
	{FlagName: "chain", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"arbitrum", "base", "bsc", "ethereum", "hyperevm", "tempo"}, Description: "Blockchain network. (options: arbitrum, base, bsc, ethereum, hyperevm, tempo) [required]"},
	{FlagName: "protocol", FieldPath: "Protocol", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"AAVE", "EULER", "MORPHO"}, Description: "Which lending protocol a credit action targets.\n\n``AAVE`` is the default so existing callers (which never send a ``protocol``\nfield) keep hitting the unchanged Aave code path; markets are named by token\nsymbol. ``MORPHO`` identifies Morpho Blue lending markets by their bytes32\n``market_id``. ``EULER`` identifies Euler V2 markets by their EVK\n``collateral_vault`` + ``borrow_vault`` addresses and supports isolated\nper-sub-account positions (``sub_account_id``).\n\nDeployment is per chain, so a valid protocol can still 422 on a given chain:\nAAVE on Ethereum, Base, Arbitrum, BSC and HyperEVM (where it is Hyperlend, the\nchain's Aave V3 deployment); MORPHO on Ethereum, Base, Arbitrum and HyperEVM\n(where it is Felix); EULER on Ethereum, Base, Arbitrum and BSC.\n\nAll three support ``/v2/credit/loop``, ``/v2/credit/unloop``,\n``/v2/credit/looped_positions`` and ``/v2/credit/rebalance``. (options: AAVE, EULER, MORPHO)"},
	{FlagName: "collateral-vault", FieldPath: "CollateralVault", Kind: flagutil.FlagKindString, Optional: true, Description: "Euler only: the EVK collateral vault to supply into. Required when protocol=EULER and supplying collateral."},
	{FlagName: "borrow-vault", FieldPath: "BorrowVault", Kind: flagutil.FlagKindString, Optional: true, Description: "Euler only: the EVK vault to borrow from. Required when protocol=EULER."},
	{FlagName: "market-id", Shorthand: "m", FieldPath: "MarketID", Kind: flagutil.FlagKindString, Optional: true, Description: "Morpho only: the bytes32 market id (from /v2/credit/morpho_markets). Required when protocol=MORPHO."},
	{FlagName: "sub-account-id", FieldPath: "SubAccountID", Kind: flagutil.FlagKindInt64, Optional: true, HasMinimum: true, Minimum: 0, HasMaximum: true, Maximum: 255, Description: "Euler only: EVC sub-account (0–255) to isolate this position. Each sub-account is an independent Euler position with its own collateral, borrow controller, and health, letting one Credit Account hold multiple isolated Euler positions. Defaults to 0. Ignored for Aave/Morpho."},
	{FlagName: "token-in", Shorthand: "t", FieldPath: "TokenIn", Kind: flagutil.FlagKindString, Optional: true, Description: "Token currently held in the Credit Account to use as input. If the same as collateral_token, no swap is performed. Omit together with amount_in and collateral_token to borrow against existing collateral."},
	{FlagName: "amount-in", Shorthand: "a", FieldPath: "AmountIn", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "collateral-token", FieldPath: "CollateralToken", Kind: flagutil.FlagKindString, Optional: true, Description: "Aave reserve token to supply as collateral. Omit together with token_in and amount_in for borrow-only mode."},
	{FlagName: "borrow-token", FieldPath: "BorrowToken", Kind: flagutil.FlagKindString, Required: true, Description: "Asset to borrow from Aave. [required]"},
	{FlagName: "borrow-amount", FieldPath: "BorrowAmount", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "interest-rate-mode", Shorthand: "i", FieldPath: "InterestRateMode", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"stable", "variable"}, Description: "On AAVE there are 2 different interest modes.\n\nA stable (but typically higher rate), or a variable rate. (options: stable, variable)"},
	{FlagName: "slippage", FieldPath: "Slippage", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "If true, returns EIP-712 signature data instead of an unsigned transaction."},
	{FlagName: "fee", Shorthand: "f", FieldPath: "Fee", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"fee,omitempty"`, Description: "Optional fee configuration. If provided, a fee will be deducted from the borrowed amount and sent to the specified recipient address."},
	{FlagName: "permit2-signature", FieldPath: "Permit2Signature", Kind: flagutil.FlagKindString, Optional: true, Description: "The EOA owner's signature of the Permit2 PermitTransferFrom typed data. When provided, the borrow bundle will first pull token_in from the owner's EOA into the Credit Account via Permit2. Obtain by calling /v2/credit/transfer and signing the returned EIP-712 data."},
	{FlagName: "permit2-nonce", FieldPath: "Permit2Nonce", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permit2_nonce,omitempty"`, Description: "The nonce used in the Permit2 signature (from the signed typed data)."},
	{FlagName: "permit2-deadline", FieldPath: "Permit2Deadline", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permit2_deadline,omitempty"`, Description: "The deadline timestamp used in the Permit2 signature (from the signed typed data)."},
}

// initBorrowCmd initializes the borrow command.
func initBorrowCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "borrow",
		Short:   "Borrow against collateral",
		Long:    "Borrow an asset from Aave using a Credit Account.\n\nBundles an optional swap, collateral supply, and borrow into a single atomic Safe transaction.\n\n- If `token_in` equals `collateral_token`, the tokens are supplied directly as collateral.\n- If `token_in` differs from `collateral_token`, a market swap is performed first.\n\nThe Credit Account must already be created via `/v2/credit/create_account` and funded with `token_in`.",
		Example: "  compass credit borrow --owner 0xfeEb60eBf707DAA8248277058eb8D28EaeCF5425 --chain base --borrow-token WETH --borrow-amount 0.01",
		Args:    cobra.NoArgs,
		RunE:    runBorrowCmd,
		Annotations: map[string]string{
			"speakeasy_operation": "v2_credit_borrow",
		},
	}
	flagutil.RegisterFlags(cmd, borrowCmdMeta)
	if err := flagutil.ValidateMeta[components.CreditBorrowRequest](borrowCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for borrow: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin; @path reads a file, @- reads stdin to EOF. Use --schema to print the exact JSON Schema.")
	_ = flagutil.AnnotatePromptFlag(cmd, "body", flagutil.PromptFlagSpec{Kind: "json", BodyFlag: true})
	cmd.Annotations[flagutil.AnnotationWholeBodyFlag] = "body"
	if err := flagutil.AnnotateBodyFields(cmd, borrowCmdMeta, "", "body"); err != nil {
		return fmt.Errorf("annotate body fields for borrow: %w", err)
	}
	cmd.Flags().Bool("schema", false, "Print the exact JSON Schema of the request body and exit")
	_ = flagutil.AnnotatePromptFlag(cmd, "schema", flagutil.PromptFlagSpec{Kind: "bool", DocSurface: true})
	parent.AddCommand(cmd)
	return nil
}

// runBorrowCmd executes the borrow command.
func runBorrowCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if requested, _ := cmd.Flags().GetBool("schema"); requested {
		return usage.EmitBodySchema(cmd.OutOrStdout(), "v2_credit_borrow")
	}
	request, err := flagutil.BuildRequest[components.CreditBorrowRequest](cmd, borrowCmdMeta, "", "body")
	if err != nil {
		return flagutil.WithCLIValidation(err)
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Credit.Borrow(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
