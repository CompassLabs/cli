// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package credit

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var creditRebalanceCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The address that owns the Credit Account. [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"base", "ethereum", "arbitrum", "hyperevm", "tempo", "bsc"}, Description: "The chain to use. (options: base, ethereum, arbitrum, hyperevm, tempo, bsc) [required]"},
	{FlagName: "targets", Shorthand: "t", FieldPath: "Targets", Kind: flagutil.FlagKindJSON, Required: true, Annotations: `json:"targets"`, Description: "The positions to change. Scoped: only positions named here are touched; any position not listed is left untouched. [required]"},
	{FlagName: "max-slippage-percent", Shorthand: "m", FieldPath: "MaxSlippagePercent", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "If true, returns EIP-712 typed data for gas-sponsored execution instead of an unsigned transaction."},
}

// initCreditRebalanceCmd initializes the credit-rebalance command.
func initCreditRebalanceCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "credit-rebalance",
		Short:   "Rebalance the leveraged credit book",
		Long:    "Rebalance the leveraged credit book in ONE atomic transaction.\n\nList only the positions to change — anything not named is left untouched;\nremove a position with close=true. Each target states an end state:\ntarget_equity_usd (net USD committed) × target_multiplier.\n\nReleasing targets run first (each unwind/delever frees tokens into the Credit\nAccount), the freed tokens are then routed by swaps at a GUARANTEED minimum\noutput (enforced on-chain), and consuming targets run last — so moving a\nlevered position between markets, token pairs, or protocols (Aave ↔ Morpho) is\nsimply a close plus an open in the same transaction.\n\nNet book growth is funded from the Credit Account's existing idle balance —\nfund it first via /v2/credit/transfer; a net release stays in the Credit\nAccount as idle balance. Any swap surplus above the guaranteed floors also\nstays in the Credit Account (preview.estimated_max_dust) — recoverable, never\nlost.\n\nA book already at its target returns transaction: null with the preview — the\ncall is idempotent and safe to drive from a converge-to-target loop.\n\nA rebalance too large for one transaction is rejected with a 422 — split it\ninto two calls. Every deleveraging step keeps the health factor ≥ 1.02 and\nevery leveraging step respects the protocol's borrow limits; Aave targets share\none account-level health factor, which the preview reports.\n\nFor protocol=MORPHO pass a market_id from /v2/credit/morpho_markets; inspect the\ncurrent book via /v2/credit/looped_positions.",
		Example: "  compass credit credit-rebalance --owner 0x4D3c07d1db7E4A9E44285fae9810d6549655bc74 --chain ethereum --targets '[{\"protocol\":\"AAVE\",\"collateral_token\":\"WETH\",\"borrow_token\":\"USDC\",\"target_multiplier\":2}]'",
		RunE:    runCreditRebalanceCmd,
		Aliases: []string{"cr"},
	}
	flagutil.RegisterFlags(cmd, creditRebalanceCmdMeta)
	if err := flagutil.ValidateMeta[components.CreditRebalanceRequest](creditRebalanceCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for credit-rebalance: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCreditRebalanceCmd executes the credit-rebalance command.
func runCreditRebalanceCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, creditRebalanceCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, creditRebalanceCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.CreditRebalanceRequest](cmd, creditRebalanceCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Credit.CreditRebalance(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
