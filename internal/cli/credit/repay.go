// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package credit

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var repayCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The address that owns the Credit Account. [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"base", "ethereum", "arbitrum", "hyperevm"}, Description: "The chain to use. (options: base, ethereum, arbitrum, hyperevm) [required]"},
	{FlagName: "repay-token", FieldPath: "RepayToken", Kind: flagutil.FlagKindString, Required: true, Description: "The borrowed asset to repay (e.g. WETH). Must match the debt position's token. [required]"},
	{FlagName: "repay-amount", FieldPath: "RepayAmount", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "interest-rate-mode", Shorthand: "i", FieldPath: "InterestRateMode", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"stable", "variable"}, Description: "On AAVE there are 2 different interest modes.\n\nA stable (but typically higher rate), or a variable rate. (options: stable, variable)"},
	{FlagName: "withdraw-token", FieldPath: "WithdrawToken", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"withdraw_token,omitempty"`, Description: "Collateral token to withdraw from Aave after repaying debt. Omit together with withdraw_amount for repay-only mode."},
	{FlagName: "withdraw-amount", FieldPath: "WithdrawAmount", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "token-out", Shorthand: "t", FieldPath: "TokenOut", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"token_out,omitempty"`, Description: "Desired output token. If different from withdraw_token, a swap is performed after withdrawal. If None, the withdrawn collateral is kept as-is."},
	{FlagName: "slippage", Shorthand: "s", FieldPath: "Slippage", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "If true, returns EIP-712 signature data instead of an unsigned transaction."},
	{FlagName: "permit2-signature", FieldPath: "Permit2Signature", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permit2_signature,omitempty"`, Description: "The EOA owner's signature of the Permit2 PermitTransferFrom typed data. When provided, the repay bundle will first pull repay_token from the owner's EOA into the Credit Account via Permit2. Obtain by calling /v2/credit/transfer and signing the returned EIP-712 data."},
	{FlagName: "permit2-nonce", FieldPath: "Permit2Nonce", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permit2_nonce,omitempty"`, Description: "The nonce used in the Permit2 signature (from the signed typed data)."},
	{FlagName: "permit2-deadline", FieldPath: "Permit2Deadline", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permit2_deadline,omitempty"`, Description: "The deadline timestamp used in the Permit2 signature (from the signed typed data)."},
}

// initRepayCmd initializes the repay command.
func initRepayCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "repay",
		Short:   "Repay debt and withdraw collateral",
		Long:    "Repay an Aave debt and withdraw collateral from a Credit Account.\n\nBundles repayment, collateral withdrawal, and an optional swap into a single atomic Safe transaction.\n\n- If `token_out` is None or equals `withdraw_token`, the withdrawn collateral is kept as-is.\n- If `token_out` differs from `withdraw_token`, a swap is performed after withdrawal via 1inch.\n\nThe Credit Account must already have a borrow position created via `/v2/credit/borrow`.\nThe repay_token must be available in the Credit Account (or pulled from EOA via Permit2).",
		Example: "  compass credit repay --owner 0x831Ad0C52C77708DA5D49dc8278C966dfdD4ddA1 --chain base --repay-token WETH --repay-amount 0.01",
		RunE:    runRepayCmd,
	}
	flagutil.RegisterFlags(cmd, repayCmdMeta)
	if err := flagutil.ValidateMeta[components.CreditRepayRequest](repayCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for repay: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runRepayCmd executes the repay command.
func runRepayCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, repayCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, repayCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.CreditRepayRequest](cmd, repayCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Credit.Repay(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
