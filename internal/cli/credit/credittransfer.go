// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package credit

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var creditTransferCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The owner's wallet address (EOA). [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"base", "ethereum", "arbitrum", "hyperevm"}, Description: "The chain to use. (options: base, ethereum, arbitrum, hyperevm) [required]"},
	{FlagName: "token", Shorthand: "t", FieldPath: "Token", Kind: flagutil.FlagKindString, Required: true, Description: "The token to transfer. [required]"},
	{FlagName: "amount", FieldPath: "Amount", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "action", FieldPath: "Action", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"DEPOSIT", "WITHDRAW"}, Description: "Whether you are depositing to or withdrawing from your credit account. (options: DEPOSIT, WITHDRAW) [required]"},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "Optionally request gas sponsorship. If set to `true`, EIP-712 signature data will be returned that must be signed by the `owner` and submitted to the `/gas_sponsorship/prepare` endpoint."},
	{FlagName: "spender", Shorthand: "s", FieldPath: "Spender", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"spender,omitempty"`, Description: "The address that will call Permit2's permitTransferFrom to execute the transfer. When `action` is 'DEPOSIT' and `gas_sponsorship` is `true`: - If provided, the signature will authorize this address (typically a gas sponsor) to pull tokens. - If not provided, defaults to the Credit Account (Safe) address, allowing the transfer to be included in a bundle transaction where the Safe pulls the tokens itself."},
}

// initCreditTransferCmd initializes the credit-transfer command.
func initCreditTransferCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "credit-transfer",
		Short:   "Transfer tokens to/from Credit Account",
		Long:    "Transfer tokens between the owner's EOA and their Credit Account.\n\n**DEPOSIT** (EOA → Credit Account):\n- With `gas_sponsorship=true`: returns Permit2 EIP-712 typed data to sign. The gas sponsor\n  calls `permitTransferFrom` to pull tokens (1 signature).\n- With `gas_sponsorship=false`: returns an unsigned ERC-20 transfer transaction.\n\n**WITHDRAW** (Credit Account → EOA):\n- With `gas_sponsorship=true`: returns SafeTx EIP-712 typed data to sign. The gas sponsor\n  broadcasts the `execTransaction` (1 signature).\n- With `gas_sponsorship=false`: returns an unsigned `execTransaction`.",
		Example: "  compass credit credit-transfer --owner 0x4A83fec8c6A9A25Be28f3242a16dBaD0ab00f3a6 --chain base --token USDC --amount 100 --action DEPOSIT",
		RunE:    runCreditTransferCmd,
		Aliases: []string{"ct"},
	}
	flagutil.RegisterFlags(cmd, creditTransferCmdMeta)
	if err := flagutil.ValidateMeta[components.CreditTransferRequest](creditTransferCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for credit-transfer: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCreditTransferCmd executes the credit-transfer command.
func runCreditTransferCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, creditTransferCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, creditTransferCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.CreditTransferRequest](cmd, creditTransferCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Credit.CreditTransfer(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
