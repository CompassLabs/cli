// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package riskyield

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var riskYieldMintCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The wallet that owns the Risk Yield Account. [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"robinhood"}, Description: "Risk Yield is available on Robinhood Chain only. (options: robinhood)"},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "Return EIP-712 typed data for the owner to sign instead of a transaction, so a sponsor can broadcast it."},
	{FlagName: "preview", FieldPath: "Preview", Kind: flagutil.FlagKindBool, Optional: true, Description: "Return the plan and its simulation without building a transaction. Nothing is signed and nothing can be broadcast."},
	{FlagName: "slippage-pct", Shorthand: "s", FieldPath: "SlippagePct", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "deadline-seconds", FieldPath: "DeadlineSeconds", Kind: flagutil.FlagKindInt64, Optional: true, Description: "How long the transaction stays valid once built."},
	{FlagName: "pool", FieldPath: "Pool", Kind: flagutil.FlagKindJSON, Required: true, Annotations: `json:"pool"`, Description: "Which pool, by id or by its key.\n\nA pool id is stable and is what the list endpoints return. The long form\nexists so a caller who knows the pair can address a pool without a lookup —\non a chain minting twenty thousand pools a day, a client should not have to\nsearch for one it just created. [required]"},
	{FlagName: "range", FieldPath: "Range", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: true, DiscriminatorKey: "Type", TypeDescription: "JSON value (variants: full: object, prices: { price_lower: value, price_upper: value }, symmetric_pct: { width_pct: value }, ticks: { tick_lower: integer, tick_upper: integer })", Variants: []flagutil.UnionVariantMeta{
		{DiscriminatorValue: "full", FlagName: "range.full", FieldName: "FullRange", CanExpand: true, Description: "FullRange variant as JSON"},
		{DiscriminatorValue: "prices", FlagName: "range.prices", FieldName: "PriceRange", CanExpand: false, Description: "PriceRange variant as JSON"},
		{DiscriminatorValue: "symmetric_pct", FlagName: "range.symmetric-pct", FieldName: "SymmetricRange", CanExpand: false, Description: "SymmetricRange variant as JSON"},
		{DiscriminatorValue: "ticks", FlagName: "range.ticks", FieldName: "TickRange", CanExpand: true, Description: "TickRange variant as JSON", Fields: []flagutil.FlagMeta{
			{FlagName: "range.ticks.tick-lower", FieldPath: "TickLower", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
			{FlagName: "range.ticks.tick-upper", FieldPath: "TickUpper", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
		}},
	}}},
	{FlagName: "deposit", FieldPath: "Deposit", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: true, DiscriminatorKey: "Type", TypeDescription: "JSON value (variants: both: { amount0: value, amount1: value }, single: { token: string, amount: value }, usd: { value_usd: value })", Variants: []flagutil.UnionVariantMeta{
		{DiscriminatorValue: "both", FlagName: "deposit.both", FieldName: "BothSidesDeposit", CanExpand: false, Description: "BothSidesDeposit variant as JSON"},
		{DiscriminatorValue: "single", FlagName: "deposit.single", FieldName: "SingleSidedDeposit", CanExpand: false, Description: "SingleSidedDeposit variant as JSON"},
		{DiscriminatorValue: "usd", FlagName: "deposit.usd", FieldName: "UsdDeposit", CanExpand: false, Description: "UsdDeposit variant as JSON"},
	}}},
	{FlagName: "recipient", FieldPath: "Recipient", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"recipient,omitempty"`, Description: "Who receives the position NFT. Defaults to the Risk Yield Account, which is what makes it manageable through this API."},
}

// initRiskYieldMintCmd initializes the risk-yield-mint command.
func initRiskYieldMintCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "risk-yield-mint",
		Short:   "Open a position",
		Long:    "Provide liquidity to a pool over a price range.\n\nThe range you ask for is not quite the range you get: ticks are discrete, so\na 30% band becomes the usable ticks that bracket it — snapped outward, never\ninward, because a position that stops earning sooner than you expected is\nthe worse surprise. The `range` in the response is what will actually be\nopened.\n\nA range also fixes the proportion of the two assets, so a deposit rarely\nuses all of both. What does not fit is reported as `leftover` and stays in\nyour account rather than being quietly swept.\n\nSet `preview` to see the plan and its simulation without building anything.",
		Example: "  compass risk-yield risk-yield-mint --owner 0x06A9aF046187895AcFc7258450B15397CAc67400 --pool '{\"pool_id\":1}' --range '{\"type\":\"symmetric_pct\",\"width_pct\":\"30\"}' --deposit '{\"type\":\"both\",\"amount0\":\"100\",\"amount1\":\"0.04\"}'",
		RunE:    runRiskYieldMintCmd,
		Aliases: []string{"rym"},
	}
	flagutil.RegisterFlags(cmd, riskYieldMintCmdMeta)
	if err := flagutil.ValidateMeta[components.RiskYieldMintRequest](riskYieldMintCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for risk-yield-mint: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runRiskYieldMintCmd executes the risk-yield-mint command.
func runRiskYieldMintCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, riskYieldMintCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, riskYieldMintCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.RiskYieldMintRequest](cmd, riskYieldMintCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.RiskYield.RiskYieldMint(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
