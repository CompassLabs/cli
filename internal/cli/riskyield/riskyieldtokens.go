// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package riskyield

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var riskYieldTokensCmdMeta = []flagutil.FlagMeta{
	{FlagName: "offset", FieldPath: "Offset", Kind: flagutil.FlagKindInt64, Optional: true, Description: "The offset of the first item to return."},
	{FlagName: "limit", FieldPath: "Limit", Kind: flagutil.FlagKindInt64, Optional: true, Description: "The number of items to return."},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"robinhood"}, Description: "options: robinhood [required]"},
	{FlagName: "kind", Shorthand: "k", FieldPath: "Kind", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=kind"`, Description: "Filter to one kind of asset. (options: STOCK, STABLE, NATIVE, WRAPPED_NATIVE, MAJOR, LAUNCHPAD_MEME, OTHER)"},
	{FlagName: "launchpad", FieldPath: "Launchpad", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=launchpad"`, Description: "options: PONS_V1, PONS_V2, POOLS_TRADE, BAGS"},
	{FlagName: "search", FieldPath: "Search", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=search"`, Description: "Matches symbol, name or stock ticker."},
	{FlagName: "has-price", FieldPath: "HasPrice", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=has_price"`, Description: "Only tokens the indexer can currently price."},
	{FlagName: "stock-ticker", FieldPath: "StockTicker", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=stock_ticker"`, Description: "string value"},
	{FlagName: "min-liquidity-usd", Shorthand: "m", FieldPath: "MinLiquidityUsd", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "order-by", FieldPath: "OrderBy", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"liquidity_usd", "volume_24h_usd", "market_cap_usd", "price_change_24h_pct", "created_at"}, Description: "options: liquidity_usd, volume_24h_usd, market_cap_usd, price_change_24h_pct, created_at"},
	{FlagName: "direction", FieldPath: "Direction", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"asc", "desc"}, Description: "options: asc, desc"},
}

// initRiskYieldTokensCmd initializes the risk-yield-tokens command.
func initRiskYieldTokensCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "risk-yield-tokens",
		Short:   "List tokens",
		Long:    "The assets that trade on Robinhood Chain.\n\nTokenized stocks carry their issuer metadata, including the corporate-action\nmultiplier — balances do not rebase on a split, so a quote for the\nunderlying only becomes a token price once that is applied.\n\nMemecoins carry the launch they came from. Every price carries the source it\nwas resolved from and a confidence: below 0.6 it came from a pool shallow\nenough that one trade moves it, and every dollar figure derived from it is\nindicative.",
		Example: "  compass risk-yield risk-yield-tokens --chain robinhood",
		RunE:    runRiskYieldTokensCmd,
		Aliases: []string{"ryt"},
	}
	flagutil.RegisterFlags(cmd, riskYieldTokensCmdMeta)
	if err := flagutil.ValidateMeta[operations.V2RiskYieldTokensRequest](riskYieldTokensCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for risk-yield-tokens: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runRiskYieldTokensCmd executes the risk-yield-tokens command.
func runRiskYieldTokensCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, riskYieldTokensCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, riskYieldTokensCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.V2RiskYieldTokensRequest](cmd, riskYieldTokensCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.RiskYield.RiskYieldTokens(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
