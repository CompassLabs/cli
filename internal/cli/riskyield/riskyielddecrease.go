// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package riskyield

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/components"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var riskYieldDecreaseCmdMeta = []flagutil.FlagMeta{
	{FlagName: "owner", FieldPath: "Owner", Kind: flagutil.FlagKindString, Required: true, Description: "The wallet that owns the Risk Yield Account. [required]"},
	{FlagName: "chain", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"robinhood"}, Description: "Risk Yield is available on Robinhood Chain only. (options: robinhood)"},
	{FlagName: "gas-sponsorship", Shorthand: "g", FieldPath: "GasSponsorship", Kind: flagutil.FlagKindBool, Optional: true, Description: "Return EIP-712 typed data for the owner to sign instead of a transaction, so a sponsor can broadcast it."},
	{FlagName: "preview", FieldPath: "Preview", Kind: flagutil.FlagKindBool, Optional: true, Description: "Return the plan and its simulation without building a transaction. Nothing is signed and nothing can be broadcast."},
	{FlagName: "slippage-pct", Shorthand: "s", FieldPath: "SlippagePct", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "deadline-seconds", FieldPath: "DeadlineSeconds", Kind: flagutil.FlagKindInt64, Optional: true, Description: "How long the transaction stays valid once built."},
	{FlagName: "position-id", FieldPath: "PositionID", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
	{FlagName: "dex-version", FieldPath: "DexVersion", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"V3", "V4"}, Description: "Which Uniswap deployment a pool belongs to.\n\nv3 pools hold their own tokens and pay LPs the fee tier. v4 pools live in a\nshared PoolManager and may route the fee through a hook, which is where the\nlaunchpad pools' zero LP yield comes from. (options: V3, V4)"},
	{FlagName: "percent", FieldPath: "Percent", Kind: flagutil.FlagKindUnion, Union: &flagutil.UnionMeta{Discriminated: false, Optional: true, TypeDescription: "JSON value (one of: number | string)"}},
	{FlagName: "collect", FieldPath: "Collect", Kind: flagutil.FlagKindBool, Optional: true, Description: "Sweep the withdrawn tokens and any fees in the same transaction. Leave this on: `decreaseLiquidity` only credits tokens, it does not move them, so without a collect nothing reaches your account."},
	{FlagName: "burn-if-empty", Shorthand: "b", FieldPath: "BurnIfEmpty", Kind: flagutil.FlagKindBool, Optional: true, Description: "Destroy the NFT once it holds nothing. Frees a little gas."},
}

// initRiskYieldDecreaseCmd initializes the risk-yield-decrease command.
func initRiskYieldDecreaseCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "risk-yield-decrease",
		Short:   "Withdraw from a position",
		Long:    "Take liquidity back out of a position.\n\nThe collect is bundled in by default and should stay that way:\n`decreaseLiquidity` only *credits* the tokens to the position, it does not\nmove them. Without the collect the position looks emptied while the funds\nsit in the position manager.",
		Example: "  compass risk-yield risk-yield-decrease --owner 0x06A9aF046187895AcFc7258450B15397CAc67400 --position-id 1076416",
		RunE:    runRiskYieldDecreaseCmd,
		Aliases: []string{"ryd"},
	}
	flagutil.RegisterFlags(cmd, riskYieldDecreaseCmdMeta)
	if err := flagutil.ValidateMeta[components.RiskYieldDecreaseRequest](riskYieldDecreaseCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for risk-yield-decrease: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runRiskYieldDecreaseCmd executes the risk-yield-decrease command.
func runRiskYieldDecreaseCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, riskYieldDecreaseCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, riskYieldDecreaseCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.RiskYieldDecreaseRequest](cmd, riskYieldDecreaseCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.RiskYield.RiskYieldDecrease(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
