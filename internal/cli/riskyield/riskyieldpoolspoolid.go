// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package riskyield

import (
	"fmt"
	"github.com/CompassLabs/cli/internal/client"
	"github.com/CompassLabs/cli/internal/flagutil"
	"github.com/CompassLabs/cli/internal/interactive"
	"github.com/CompassLabs/cli/internal/output"
	"github.com/CompassLabs/cli/internal/sdk"
	"github.com/CompassLabs/cli/internal/sdk/models/operations"
	"github.com/CompassLabs/cli/internal/usage"
	"github.com/spf13/cobra"
)

var riskYieldPoolsPoolIDCmdMeta = []flagutil.FlagMeta{
	{FlagName: "pool-id", Shorthand: "p", FieldPath: "PoolID", Kind: flagutil.FlagKindInt64, Required: true, Description: "The pool id, as returned by `GET /pools`. [required]"},
	{FlagName: "chain", Shorthand: "c", FieldPath: "Chain", Kind: flagutil.FlagKindEnum, Required: true, EnumValues: []string{"robinhood"}, Description: "options: robinhood [required]"},
	{FlagName: "depth-words", FieldPath: "DepthWords", Kind: flagutil.FlagKindInt64, Optional: true, Description: "How many 256-tick words either side of the current price to read for the liquidity distribution. More is a wider picture and more node calls."},
	{FlagName: "history", FieldPath: "History", Kind: flagutil.FlagKindEnum, Optional: true, EnumValues: []string{"24h", "7d", "30d"}, Description: "options: 24h, 7d, 30d"},
}

// initRiskYieldPoolsPoolIdCmd initializes the risk-yield-pools-pool-id command.
func initRiskYieldPoolsPoolIdCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "risk-yield-pools-pool-id",
		Short:   "Get pool detail",
		Long:    "One pool, with its price history and the caveats that apply to it.\n\nFor a pool holding a tokenized stock, `stock_reference` compares the pool's\nprice against the stock's own. They drift: the pool trades around the clock\nwhile the stock trades in a session, and that gap is what a liquidity\nprovider is quoting against overnight.",
		Example: "  compass risk-yield risk-yield-pools-pool-id --pool-id 174842 --chain robinhood",
		RunE:    runRiskYieldPoolsPoolIdCmd,
		Aliases: []string{"ryppi"},
	}
	flagutil.RegisterFlags(cmd, riskYieldPoolsPoolIDCmdMeta)
	if err := flagutil.ValidateMeta[operations.V2RiskYieldPoolsPoolIDRequest](riskYieldPoolsPoolIDCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for risk-yield-pools-pool-id: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runRiskYieldPoolsPoolIdCmd executes the risk-yield-pools-pool-id command.
func runRiskYieldPoolsPoolIdCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, riskYieldPoolsPoolIDCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, riskYieldPoolsPoolIDCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.V2RiskYieldPoolsPoolIDRequest](cmd, riskYieldPoolsPoolIDCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.RiskYield.RiskYieldPoolsPoolID(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
