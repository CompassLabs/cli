// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// Generated under the AGPL-3.0-only license.
// SPDX-License-Identifier: AGPL-3.0-only

package components

import (
	"errors"
	"fmt"
	"github.com/CompassLabs/cli/internal/sdk/optionalnullable"
	"github.com/CompassLabs/cli/internal/sdk/sdkinternal/utils"
	"github.com/CompassLabs/cli/internal/sdk/types"
)

type CreditRepayParamsAmountType string

const (
	CreditRepayParamsAmountTypeNumber CreditRepayParamsAmountType = "number"
	CreditRepayParamsAmountTypeStr    CreditRepayParamsAmountType = "str"
)

// CreditRepayParamsAmount - Amount in token units to repay.
type CreditRepayParamsAmount struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type CreditRepayParamsAmountType
}

func CreateCreditRepayParamsAmountNumber(number float64) CreditRepayParamsAmount {
	typ := CreditRepayParamsAmountTypeNumber

	return CreditRepayParamsAmount{
		Number: &number,
		Type:   typ,
	}
}

func CreateCreditRepayParamsAmountStr(str string) CreditRepayParamsAmount {
	typ := CreditRepayParamsAmountTypeStr

	return CreditRepayParamsAmount{
		Str:  &str,
		Type: typ,
	}
}

func (u *CreditRepayParamsAmount) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditRepayParamsAmountTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditRepayParamsAmountTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditRepayParamsAmount", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditRepayParamsAmount", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(CreditRepayParamsAmountType)
	switch best.Type {
	case CreditRepayParamsAmountTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case CreditRepayParamsAmountTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditRepayParamsAmount", string(data))
}

func (u CreditRepayParamsAmount) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type CreditRepayParamsAmount: all fields are null")
}

// CreditRepayParams - Repay debt to Aave via Credit Account.
type CreditRepayParams struct {
	//lint:ignore U1000 accessed via reflection for JSON marshaling
	actionType *string `const:"CREDIT_REPAY" json:"action_type,omitzero"`
	// Token to repay.
	Token string `json:"token"`
	// Amount in token units to repay.
	Amount CreditRepayParamsAmount `json:"amount"`
	// On AAVE there are 2 different interest modes.
	//
	// A stable (but typically higher rate), or a variable rate.
	InterestRateMode *InterestRateMode `json:"interest_rate_mode,omitzero"`
	// Which lending protocol a credit action targets.
	//
	// ``AAVE`` is the default so existing callers (which never send a ``protocol``
	// field) keep hitting the unchanged Aave code path; markets are named by token
	// symbol. ``MORPHO`` identifies Morpho Blue lending markets by their bytes32
	// ``market_id``. ``EULER`` identifies Euler V2 markets by their EVK
	// ``collateral_vault`` + ``borrow_vault`` addresses and supports isolated
	// per-sub-account positions (``sub_account_id``).
	//
	// Deployment is per chain, so a valid protocol can still 422 on a given chain:
	// AAVE on Ethereum, Base, Arbitrum, BSC and HyperEVM (where it is Hyperlend, the
	// chain's Aave V3 deployment); MORPHO on Ethereum, Base, Arbitrum and HyperEVM
	// (where it is Felix); EULER on Ethereum, Base, Arbitrum and BSC.
	//
	// All three support ``/v2/credit/loop`` and ``/v2/credit/unloop``. EULER does
	// NOT: ``/v2/credit/rebalance`` rejects it with a 422, and
	// ``/v2/credit/looped_positions`` covers only AAVE and MORPHO — an Euler loop is
	// silently absent there rather than an error, so read it from
	// ``/v2/credit/positions`` instead. (EULER still appears in the
	// ``looped_positions`` response enum because this enum is shared; it is never
	// emitted.)
	Protocol *CreditProtocol `json:"protocol,omitzero"`
	// Euler only: the EVK vault the debt is owed to. Required when protocol=EULER.
	BorrowVault optionalnullable.OptionalNullable[string] `json:"borrow_vault,omitzero"`
	// Euler only: EVC sub-account (0–255) holding the debt to repay. Each sub-account is an independent Euler position with its own collateral, borrow controller, and health. Defaults to 0. Ignored for Aave/Morpho.
	SubAccountID *int64 `json:"sub_account_id,omitzero"`
	// Morpho only: the bytes32 market id (from /v2/credit/morpho_markets). Required when protocol=MORPHO.
	MarketID optionalnullable.OptionalNullable[string] `json:"market_id,omitzero"`
}

func (c CreditRepayParams) MarshalJSON() ([]byte, error) {
	return utils.MarshalJSON(c, "", false)
}

func (c *CreditRepayParams) UnmarshalJSON(data []byte) error {
	if err := utils.UnmarshalJSON(data, &c, "", false, nil); err != nil {
		return err
	}
	return nil
}

func (c *CreditRepayParams) GetActionType() *string {
	return types.Pointer("CREDIT_REPAY")
}

func (c *CreditRepayParams) GetToken() string {
	if c == nil {
		return ""
	}
	return c.Token
}

func (c *CreditRepayParams) GetAmount() CreditRepayParamsAmount {
	if c == nil {
		return CreditRepayParamsAmount{}
	}
	return c.Amount
}

func (c *CreditRepayParams) GetInterestRateMode() *InterestRateMode {
	if c == nil {
		return nil
	}
	return c.InterestRateMode
}

func (c *CreditRepayParams) GetProtocol() *CreditProtocol {
	if c == nil {
		return nil
	}
	return c.Protocol
}

func (c *CreditRepayParams) GetBorrowVault() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.BorrowVault
}

func (c *CreditRepayParams) GetSubAccountID() *int64 {
	if c == nil {
		return nil
	}
	return c.SubAccountID
}

func (c *CreditRepayParams) GetMarketID() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.MarketID
}
