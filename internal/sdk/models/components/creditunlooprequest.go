// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// Generated under the AGPL-3.0-only license.
// SPDX-License-Identifier: AGPL-3.0-only

package components

import (
	"encoding/json"
	"errors"
	"fmt"
	"github.com/CompassLabs/cli/internal/sdk/optionalnullable"
	"github.com/CompassLabs/cli/internal/sdk/sdkinternal/utils"
)

// CreditUnloopRequestChain - Blockchain network. Not every protocol is deployed on every chain — see the protocol field — and a chain with no credit venue at all returns a 422 naming the chains that do.
type CreditUnloopRequestChain string

const (
	CreditUnloopRequestChainArbitrum CreditUnloopRequestChain = "arbitrum"
	CreditUnloopRequestChainBase     CreditUnloopRequestChain = "base"
	CreditUnloopRequestChainBsc      CreditUnloopRequestChain = "bsc"
	CreditUnloopRequestChainEthereum CreditUnloopRequestChain = "ethereum"
	CreditUnloopRequestChainHyperevm CreditUnloopRequestChain = "hyperevm"
	CreditUnloopRequestChainTempo    CreditUnloopRequestChain = "tempo"
)

func (e CreditUnloopRequestChain) ToPointer() *CreditUnloopRequestChain {
	return &e
}
func (e *CreditUnloopRequestChain) UnmarshalJSON(data []byte) error {
	var v string
	if err := json.Unmarshal(data, &v); err != nil {
		return err
	}
	switch v {
	case "arbitrum":
		fallthrough
	case "base":
		fallthrough
	case "bsc":
		fallthrough
	case "ethereum":
		fallthrough
	case "hyperevm":
		fallthrough
	case "tempo":
		*e = CreditUnloopRequestChain(v)
		return nil
	default:
		return fmt.Errorf("invalid value for CreditUnloopRequestChain: %v", v)
	}
}

type CreditUnloopRequestTargetMultiplierType string

const (
	CreditUnloopRequestTargetMultiplierTypeNumber CreditUnloopRequestTargetMultiplierType = "number"
	CreditUnloopRequestTargetMultiplierTypeStr    CreditUnloopRequestTargetMultiplierType = "str"
)

// CreditUnloopRequestTargetMultiplier - Target leverage after the unwind. Omit (null) for a FULL unwind: the debt is cleared exactly — accrued interest included — and all pair collateral is withdrawn back to the Credit Account. `1` runs the same exact debt close but leaves the collateral supplied (earning). A value greater than 1 delevers the position to that multiplier. Must be below the position's current multiplier.
type CreditUnloopRequestTargetMultiplier struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type CreditUnloopRequestTargetMultiplierType
}

func CreateCreditUnloopRequestTargetMultiplierNumber(number float64) CreditUnloopRequestTargetMultiplier {
	typ := CreditUnloopRequestTargetMultiplierTypeNumber

	return CreditUnloopRequestTargetMultiplier{
		Number: &number,
		Type:   typ,
	}
}

func CreateCreditUnloopRequestTargetMultiplierStr(str string) CreditUnloopRequestTargetMultiplier {
	typ := CreditUnloopRequestTargetMultiplierTypeStr

	return CreditUnloopRequestTargetMultiplier{
		Str:  &str,
		Type: typ,
	}
}

func (u *CreditUnloopRequestTargetMultiplier) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditUnloopRequestTargetMultiplierTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditUnloopRequestTargetMultiplierTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestTargetMultiplier", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestTargetMultiplier", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(CreditUnloopRequestTargetMultiplierType)
	switch best.Type {
	case CreditUnloopRequestTargetMultiplierTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case CreditUnloopRequestTargetMultiplierTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestTargetMultiplier", string(data))
}

func (u CreditUnloopRequestTargetMultiplier) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type CreditUnloopRequestTargetMultiplier: all fields are null")
}

type CreditUnloopRequestMaxSlippagePercentType string

const (
	CreditUnloopRequestMaxSlippagePercentTypeNumber CreditUnloopRequestMaxSlippagePercentType = "number"
	CreditUnloopRequestMaxSlippagePercentTypeStr    CreditUnloopRequestMaxSlippagePercentType = "str"
)

// CreditUnloopRequestMaxSlippagePercent - Per-swap slippage tolerance in percent. Unwind dust is bounded by this per iteration, so tighter slippage means less dust.
type CreditUnloopRequestMaxSlippagePercent struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type CreditUnloopRequestMaxSlippagePercentType
}

func CreateCreditUnloopRequestMaxSlippagePercentNumber(number float64) CreditUnloopRequestMaxSlippagePercent {
	typ := CreditUnloopRequestMaxSlippagePercentTypeNumber

	return CreditUnloopRequestMaxSlippagePercent{
		Number: &number,
		Type:   typ,
	}
}

func CreateCreditUnloopRequestMaxSlippagePercentStr(str string) CreditUnloopRequestMaxSlippagePercent {
	typ := CreditUnloopRequestMaxSlippagePercentTypeStr

	return CreditUnloopRequestMaxSlippagePercent{
		Str:  &str,
		Type: typ,
	}
}

func (u *CreditUnloopRequestMaxSlippagePercent) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditUnloopRequestMaxSlippagePercentTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditUnloopRequestMaxSlippagePercentTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestMaxSlippagePercent", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestMaxSlippagePercent", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(CreditUnloopRequestMaxSlippagePercentType)
	switch best.Type {
	case CreditUnloopRequestMaxSlippagePercentTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case CreditUnloopRequestMaxSlippagePercentTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditUnloopRequestMaxSlippagePercent", string(data))
}

func (u CreditUnloopRequestMaxSlippagePercent) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type CreditUnloopRequestMaxSlippagePercent: all fields are null")
}

// CreditUnloopRequestPricing - Swap-leg routing policy. 'auto': firm quotes where a firm venue covers the pair, transparent fallback to the market aggregator otherwise. 'firm': never price on the market route — previews the firm venue cannot serve return the firm_available advisory alone (preview=null, zero aggregator calls), and executions fail with a typed error instead of silently substituting market pricing. 'market': never route through the firm venue; every leg is priced by the aggregator. max_slippage_percent applies on EVERY policy — unlike the loop, firm unwinds consume it to size the guaranteed withdraw/repay floors (the fills themselves are exact). 'firm' is incompatible with gas_sponsorship (sponsored unwinds force market routing).
type CreditUnloopRequestPricing string

const (
	CreditUnloopRequestPricingAuto   CreditUnloopRequestPricing = "auto"
	CreditUnloopRequestPricingFirm   CreditUnloopRequestPricing = "firm"
	CreditUnloopRequestPricingMarket CreditUnloopRequestPricing = "market"
)

func (e CreditUnloopRequestPricing) ToPointer() *CreditUnloopRequestPricing {
	return &e
}
func (e *CreditUnloopRequestPricing) UnmarshalJSON(data []byte) error {
	var v string
	if err := json.Unmarshal(data, &v); err != nil {
		return err
	}
	switch v {
	case "auto":
		fallthrough
	case "firm":
		fallthrough
	case "market":
		*e = CreditUnloopRequestPricing(v)
		return nil
	default:
		return fmt.Errorf("invalid value for CreditUnloopRequestPricing: %v", v)
	}
}

// CreditUnloopRequest - Unwind a leveraged loop: repeatedly withdraw collateral, swap it back to
// the borrow token at a guaranteed minimum output, and repay — all in ONE
// atomic transaction from the Credit Account.
type CreditUnloopRequest struct {
	// The address that owns the Credit Account.
	Owner string `json:"owner"`
	// Blockchain network. Not every protocol is deployed on every chain — see the protocol field — and a chain with no credit venue at all returns a 422 naming the chains that do.
	Chain CreditUnloopRequestChain `json:"chain"`
	// Which lending protocol a credit action targets.
	//
	// ``AAVE`` is the default so existing callers (which never send a ``protocol``
	// field) keep hitting the unchanged Aave code path; markets are named by token
	// symbol. ``MORPHO`` identifies Morpho Blue lending markets by their bytes32
	// ``market_id``. ``EULER`` identifies Euler V2 markets by their EVK
	// ``collateral_vault`` + ``borrow_vault`` addresses and supports isolated
	// per-sub-account positions (``sub_account_id``).
	//
	// Deployment is per chain, so a valid protocol can still 422 on a given chain:
	// AAVE on Ethereum, Base, Arbitrum, BSC and HyperEVM (where it is Hyperlend, the
	// chain's Aave V3 deployment); MORPHO on Ethereum, Base, Arbitrum and HyperEVM
	// (where it is Felix); EULER on Ethereum, Base, Arbitrum and BSC.
	//
	// All three support ``/v2/credit/loop`` and ``/v2/credit/unloop``. EULER does
	// NOT: ``/v2/credit/rebalance`` rejects it with a 422, and
	// ``/v2/credit/looped_positions`` covers only AAVE and MORPHO — an Euler loop is
	// silently absent there rather than an error, so read it from
	// ``/v2/credit/positions`` instead. (EULER still appears in the
	// ``looped_positions`` response enum because this enum is shared; it is never
	// emitted.)
	Protocol *CreditProtocol `json:"protocol,omitzero"`
	// Morpho only: the bytes32 market id (from /v2/credit/morpho_markets). Required when protocol=MORPHO.
	MarketID optionalnullable.OptionalNullable[string] `json:"market_id,omitzero"`
	// Euler only: the EVK vault the loop's collateral is in. Required when protocol=EULER.
	CollateralVault optionalnullable.OptionalNullable[string] `json:"collateral_vault,omitzero"`
	// Euler only: the EVK vault the loop borrowed from (the sub-account's controller). Required when protocol=EULER.
	BorrowVault optionalnullable.OptionalNullable[string] `json:"borrow_vault,omitzero"`
	// Euler only: the EVC sub-account (0-255) holding the looped position to unwind. 0 is the Credit Account itself.
	SubAccountID *int64 `json:"sub_account_id,omitzero"`
	// Token supplied as collateral in the loop being unwound. For MORPHO it must be the market's collateral token.
	CollateralToken string `json:"collateral_token"`
	// Token borrowed in the loop; withdrawn collateral is swapped back to it and used to repay. For MORPHO it must be the market's loan token.
	BorrowToken string `json:"borrow_token"`
	// Target leverage after the unwind. Omit (null) for a FULL unwind: the debt is cleared exactly — accrued interest included — and all pair collateral is withdrawn back to the Credit Account. `1` runs the same exact debt close but leaves the collateral supplied (earning). A value greater than 1 delevers the position to that multiplier. Must be below the position's current multiplier.
	TargetMultiplier optionalnullable.OptionalNullable[CreditUnloopRequestTargetMultiplier] `json:"target_multiplier,omitzero"`
	// Per-swap slippage tolerance in percent. Unwind dust is bounded by this per iteration, so tighter slippage means less dust.
	MaxSlippagePercent *CreditUnloopRequestMaxSlippagePercent `json:"max_slippage_percent,omitzero"`
	// If the target cannot be reached in one transaction, return the maximum-progress plan (preview.fully_unwound=false) instead of a 400. This can happen because every withdrawal is sized to keep the health factor at or above 1.02 at that step, so a position opened very close to its liquidation threshold runs out of headroom before the target is met. A second unloop call, now from a much lower leverage, finishes the job.
	AllowPartial *bool `json:"allow_partial,omitzero"`
	// If true, returns EIP-712 typed data for gas-sponsored execution instead of an unsigned transaction.
	GasSponsorship *bool `json:"gas_sponsorship,omitzero"`
	// If true, build a display estimate only — no single-use firm quote is ever spent. NOTE that this guarantees only that no firm quote was spent — it does not guarantee an absent transaction: on an unwind no firm provider covers, and under pricing=market, the call still falls through to the aggregator and returns a signable transaction. On a firm-covered unwind (pricing 'auto' or 'firm') the numbers are firm-INDICATIVE, priced off the firm provider's live maker levels; otherwise they are market-priced with slippage-bounded floors. Under pricing='firm' a preview no firm provider can serve returns the advisory alone (preview=null + firm_available). Set it on every parameter-exploration call and omit it only on the build the user is about to sign.
	Preview *bool `json:"preview,omitzero"`
	// Swap-leg routing policy. 'auto': firm quotes where a firm venue covers the pair, transparent fallback to the market aggregator otherwise. 'firm': never price on the market route — previews the firm venue cannot serve return the firm_available advisory alone (preview=null, zero aggregator calls), and executions fail with a typed error instead of silently substituting market pricing. 'market': never route through the firm venue; every leg is priced by the aggregator. max_slippage_percent applies on EVERY policy — unlike the loop, firm unwinds consume it to size the guaranteed withdraw/repay floors (the fills themselves are exact). 'firm' is incompatible with gas_sponsorship (sponsored unwinds force market routing).
	Pricing *CreditUnloopRequestPricing `json:"pricing,omitzero"`
}

func (c *CreditUnloopRequest) GetOwner() string {
	if c == nil {
		return ""
	}
	return c.Owner
}

func (c *CreditUnloopRequest) GetChain() CreditUnloopRequestChain {
	if c == nil {
		return CreditUnloopRequestChain("")
	}
	return c.Chain
}

func (c *CreditUnloopRequest) GetProtocol() *CreditProtocol {
	if c == nil {
		return nil
	}
	return c.Protocol
}

func (c *CreditUnloopRequest) GetMarketID() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.MarketID
}

func (c *CreditUnloopRequest) GetCollateralVault() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.CollateralVault
}

func (c *CreditUnloopRequest) GetBorrowVault() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.BorrowVault
}

func (c *CreditUnloopRequest) GetSubAccountID() *int64 {
	if c == nil {
		return nil
	}
	return c.SubAccountID
}

func (c *CreditUnloopRequest) GetCollateralToken() string {
	if c == nil {
		return ""
	}
	return c.CollateralToken
}

func (c *CreditUnloopRequest) GetBorrowToken() string {
	if c == nil {
		return ""
	}
	return c.BorrowToken
}

func (c *CreditUnloopRequest) GetTargetMultiplier() optionalnullable.OptionalNullable[CreditUnloopRequestTargetMultiplier] {
	if c == nil {
		return nil
	}
	return c.TargetMultiplier
}

func (c *CreditUnloopRequest) GetMaxSlippagePercent() *CreditUnloopRequestMaxSlippagePercent {
	if c == nil {
		return nil
	}
	return c.MaxSlippagePercent
}

func (c *CreditUnloopRequest) GetAllowPartial() *bool {
	if c == nil {
		return nil
	}
	return c.AllowPartial
}

func (c *CreditUnloopRequest) GetGasSponsorship() *bool {
	if c == nil {
		return nil
	}
	return c.GasSponsorship
}

func (c *CreditUnloopRequest) GetPreview() *bool {
	if c == nil {
		return nil
	}
	return c.Preview
}

func (c *CreditUnloopRequest) GetPricing() *CreditUnloopRequestPricing {
	if c == nil {
		return nil
	}
	return c.Pricing
}
