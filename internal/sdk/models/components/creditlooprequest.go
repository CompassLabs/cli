// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// Generated under the AGPL-3.0-only license.
// SPDX-License-Identifier: AGPL-3.0-only

package components

import (
	"encoding/json"
	"errors"
	"fmt"
	"github.com/CompassLabs/cli/internal/sdk/optionalnullable"
	"github.com/CompassLabs/cli/internal/sdk/sdkinternal/utils"
)

// CreditLoopRequestChain - Blockchain network.
type CreditLoopRequestChain string

const (
	CreditLoopRequestChainArbitrum CreditLoopRequestChain = "arbitrum"
	CreditLoopRequestChainBase     CreditLoopRequestChain = "base"
	CreditLoopRequestChainBsc      CreditLoopRequestChain = "bsc"
	CreditLoopRequestChainEthereum CreditLoopRequestChain = "ethereum"
	CreditLoopRequestChainHyperevm CreditLoopRequestChain = "hyperevm"
	CreditLoopRequestChainTempo    CreditLoopRequestChain = "tempo"
)

func (e CreditLoopRequestChain) ToPointer() *CreditLoopRequestChain {
	return &e
}
func (e *CreditLoopRequestChain) UnmarshalJSON(data []byte) error {
	var v string
	if err := json.Unmarshal(data, &v); err != nil {
		return err
	}
	switch v {
	case "arbitrum":
		fallthrough
	case "base":
		fallthrough
	case "bsc":
		fallthrough
	case "ethereum":
		fallthrough
	case "hyperevm":
		fallthrough
	case "tempo":
		*e = CreditLoopRequestChain(v)
		return nil
	default:
		return fmt.Errorf("invalid value for CreditLoopRequestChain: %v", v)
	}
}

type InitialCollateralAmountType string

const (
	InitialCollateralAmountTypeNumber InitialCollateralAmountType = "number"
	InitialCollateralAmountTypeStr    InitialCollateralAmountType = "str"
)

// InitialCollateralAmount - Collateral (in token units) already held in the Credit Account to seed the loop.
type InitialCollateralAmount struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type InitialCollateralAmountType
}

func CreateInitialCollateralAmountNumber(number float64) InitialCollateralAmount {
	typ := InitialCollateralAmountTypeNumber

	return InitialCollateralAmount{
		Number: &number,
		Type:   typ,
	}
}

func CreateInitialCollateralAmountStr(str string) InitialCollateralAmount {
	typ := InitialCollateralAmountTypeStr

	return InitialCollateralAmount{
		Str:  &str,
		Type: typ,
	}
}

func (u *InitialCollateralAmount) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  InitialCollateralAmountTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  InitialCollateralAmountTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for InitialCollateralAmount", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for InitialCollateralAmount", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(InitialCollateralAmountType)
	switch best.Type {
	case InitialCollateralAmountTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case InitialCollateralAmountTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for InitialCollateralAmount", string(data))
}

func (u InitialCollateralAmount) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type InitialCollateralAmount: all fields are null")
}

type MultiplierType string

const (
	MultiplierTypeNumber MultiplierType = "number"
	MultiplierTypeStr    MultiplierType = "str"
)

// Multiplier - Target leverage: total collateral exposure = multiplier × initial_collateral_amount. Must be achievable at the requested loan_to_value (max ≈ 0.9 / (1 − LTV)).
type Multiplier struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type MultiplierType
}

func CreateMultiplierNumber(number float64) Multiplier {
	typ := MultiplierTypeNumber

	return Multiplier{
		Number: &number,
		Type:   typ,
	}
}

func CreateMultiplierStr(str string) Multiplier {
	typ := MultiplierTypeStr

	return Multiplier{
		Str:  &str,
		Type: typ,
	}
}

func (u *Multiplier) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  MultiplierTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  MultiplierTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for Multiplier", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for Multiplier", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(MultiplierType)
	switch best.Type {
	case MultiplierTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case MultiplierTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for Multiplier", string(data))
}

func (u Multiplier) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type Multiplier: all fields are null")
}

type LoanToValueType string

const (
	LoanToValueTypeNumber LoanToValueType = "number"
	LoanToValueTypeStr    LoanToValueType = "str"
)

// LoanToValue - Per-iteration borrow LTV in percent. Must not exceed the protocol's maximum for the market (Aave reserve/e-mode LTV; Morpho LLTV with a safety margin); borrows are sized slightly inside the requested value so no leg sits on the protocol's revert boundary.
type LoanToValue struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type LoanToValueType
}

func CreateLoanToValueNumber(number float64) LoanToValue {
	typ := LoanToValueTypeNumber

	return LoanToValue{
		Number: &number,
		Type:   typ,
	}
}

func CreateLoanToValueStr(str string) LoanToValue {
	typ := LoanToValueTypeStr

	return LoanToValue{
		Str:  &str,
		Type: typ,
	}
}

func (u *LoanToValue) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  LoanToValueTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  LoanToValueTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for LoanToValue", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for LoanToValue", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(LoanToValueType)
	switch best.Type {
	case LoanToValueTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case LoanToValueTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for LoanToValue", string(data))
}

func (u LoanToValue) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type LoanToValue: all fields are null")
}

type CreditLoopRequestMaxSlippagePercentType string

const (
	CreditLoopRequestMaxSlippagePercentTypeNumber CreditLoopRequestMaxSlippagePercentType = "number"
	CreditLoopRequestMaxSlippagePercentTypeStr    CreditLoopRequestMaxSlippagePercentType = "str"
)

// CreditLoopRequestMaxSlippagePercent - Per-swap slippage tolerance in percent. Loop dust is bounded by this per iteration, so tighter slippage means less dust.
type CreditLoopRequestMaxSlippagePercent struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type CreditLoopRequestMaxSlippagePercentType
}

func CreateCreditLoopRequestMaxSlippagePercentNumber(number float64) CreditLoopRequestMaxSlippagePercent {
	typ := CreditLoopRequestMaxSlippagePercentTypeNumber

	return CreditLoopRequestMaxSlippagePercent{
		Number: &number,
		Type:   typ,
	}
}

func CreateCreditLoopRequestMaxSlippagePercentStr(str string) CreditLoopRequestMaxSlippagePercent {
	typ := CreditLoopRequestMaxSlippagePercentTypeStr

	return CreditLoopRequestMaxSlippagePercent{
		Str:  &str,
		Type: typ,
	}
}

func (u *CreditLoopRequestMaxSlippagePercent) UnmarshalJSON(data []byte) error {

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditLoopRequestMaxSlippagePercentTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  CreditLoopRequestMaxSlippagePercentTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditLoopRequestMaxSlippagePercent", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditLoopRequestMaxSlippagePercent", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(CreditLoopRequestMaxSlippagePercentType)
	switch best.Type {
	case CreditLoopRequestMaxSlippagePercentTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case CreditLoopRequestMaxSlippagePercentTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for CreditLoopRequestMaxSlippagePercent", string(data))
}

func (u CreditLoopRequestMaxSlippagePercent) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type CreditLoopRequestMaxSlippagePercent: all fields are null")
}

// Pricing - Swap-leg routing policy. 'auto': firm quotes where a firm venue covers the pair, transparent fallback to the market aggregator otherwise. 'firm': never price on the market route — previews whose target the firm venue cannot serve return the coverage advisory alone (preview=null, zero aggregator calls), and executions fail with a typed error instead of silently substituting market pricing. 'market': never route through the firm venue; every leg is priced by the aggregator and bounded by max_slippage_percent (which firm legs ignore). 'firm' is incompatible with gas_sponsorship (sponsored loops force market routing).
type Pricing string

const (
	PricingAuto   Pricing = "auto"
	PricingFirm   Pricing = "firm"
	PricingMarket Pricing = "market"
)

func (e Pricing) ToPointer() *Pricing {
	return &e
}
func (e *Pricing) UnmarshalJSON(data []byte) error {
	var v string
	if err := json.Unmarshal(data, &v); err != nil {
		return err
	}
	switch v {
	case "auto":
		fallthrough
	case "firm":
		fallthrough
	case "market":
		*e = Pricing(v)
		return nil
	default:
		return fmt.Errorf("invalid value for Pricing: %v", v)
	}
}

// CreditLoopRequest - Open a leveraged loop: repeatedly supply collateral, borrow, and swap the
// borrow back to collateral — all in ONE atomic transaction from the Credit
// Account.
type CreditLoopRequest struct {
	// The address that owns the Credit Account.
	Owner string `json:"owner"`
	// Blockchain network.
	Chain CreditLoopRequestChain `json:"chain"`
	// Which lending protocol a credit action targets.
	//
	// ``AAVE`` is the default so existing callers (which never send a ``protocol``
	// field) keep hitting the unchanged Aave code path. ``MORPHO`` identifies Morpho
	// Blue lending markets by their bytes32 ``market_id``. ``EULER`` identifies Euler
	// V2 markets by their EVK ``collateral_vault`` + ``borrow_vault`` addresses and
	// supports isolated per-sub-account positions (``sub_account_id``). All three
	// support the loop/unloop leverage endpoints.
	Protocol *CreditProtocol `json:"protocol,omitzero"`
	// Morpho only: the bytes32 market id (from /v2/credit/morpho_markets). Required when protocol=MORPHO.
	MarketID optionalnullable.OptionalNullable[string] `json:"market_id,omitzero"`
	// Euler only: the EVK vault address collateral is supplied to (from /v2/credit/euler_markets). Required when protocol=EULER.
	CollateralVault optionalnullable.OptionalNullable[string] `json:"collateral_vault,omitzero"`
	// Euler only: the EVK vault address borrowed from (the sub-account's controller). Required when protocol=EULER.
	BorrowVault optionalnullable.OptionalNullable[string] `json:"borrow_vault,omitzero"`
	// Euler only: the EVC sub-account (0-255) holding this isolated looped position. 0 is the Credit Account itself.
	SubAccountID *int64 `json:"sub_account_id,omitzero"`
	// Token supplied as collateral each iteration. Must already be in the Credit Account for the initial amount. For MORPHO it must be the market's collateral token.
	CollateralToken string `json:"collateral_token"`
	// Token borrowed each iteration and swapped back to the collateral token. For MORPHO it must be the market's loan token.
	BorrowToken string `json:"borrow_token"`
	// Collateral (in token units) already held in the Credit Account to seed the loop.
	InitialCollateralAmount InitialCollateralAmount `json:"initial_collateral_amount"`
	// Target leverage: total collateral exposure = multiplier × initial_collateral_amount. Must be achievable at the requested loan_to_value (max ≈ 0.9 / (1 − LTV)).
	Multiplier Multiplier `json:"multiplier"`
	// Per-iteration borrow LTV in percent. Must not exceed the protocol's maximum for the market (Aave reserve/e-mode LTV; Morpho LLTV with a safety margin); borrows are sized slightly inside the requested value so no leg sits on the protocol's revert boundary.
	LoanToValue LoanToValue `json:"loan_to_value"`
	// Per-swap slippage tolerance in percent. Loop dust is bounded by this per iteration, so tighter slippage means less dust.
	MaxSlippagePercent *CreditLoopRequestMaxSlippagePercent `json:"max_slippage_percent,omitzero"`
	// Aave only: e-mode category to enable before looping (higher LTV for correlated pairs, e.g. ETH-correlated).
	EmodeCategory optionalnullable.OptionalNullable[int64] `json:"emode_category,omitzero"`
	// If true, returns EIP-712 typed data for gas-sponsored execution instead of an unsigned transaction.
	GasSponsorship *bool `json:"gas_sponsorship,omitzero"`
	// If true, build a display ESTIMATE: no firm RFQ quote is ever requested (quote_expires_at stays null) and no transaction is returned. How the estimate is priced follows `pricing`: on a firm-covered pair under 'auto' or 'firm' it is computed from the firm venue's live price levels (swap_provider='bebop', indicative); otherwise swap legs are priced by the default aggregator. Set it on every call made while a user is exploring parameters, and leave it false only for the build they actually intend to sign — firm quotes are single-use maker commitments, and requesting them for displays that are never executed degrades the pricing this API is offered.
	Preview *bool `json:"preview,omitzero"`
	// Swap-leg routing policy. 'auto': firm quotes where a firm venue covers the pair, transparent fallback to the market aggregator otherwise. 'firm': never price on the market route — previews whose target the firm venue cannot serve return the coverage advisory alone (preview=null, zero aggregator calls), and executions fail with a typed error instead of silently substituting market pricing. 'market': never route through the firm venue; every leg is priced by the aggregator and bounded by max_slippage_percent (which firm legs ignore). 'firm' is incompatible with gas_sponsorship (sponsored loops force market routing).
	Pricing *Pricing `json:"pricing,omitzero"`
}

func (c *CreditLoopRequest) GetOwner() string {
	if c == nil {
		return ""
	}
	return c.Owner
}

func (c *CreditLoopRequest) GetChain() CreditLoopRequestChain {
	if c == nil {
		return CreditLoopRequestChain("")
	}
	return c.Chain
}

func (c *CreditLoopRequest) GetProtocol() *CreditProtocol {
	if c == nil {
		return nil
	}
	return c.Protocol
}

func (c *CreditLoopRequest) GetMarketID() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.MarketID
}

func (c *CreditLoopRequest) GetCollateralVault() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.CollateralVault
}

func (c *CreditLoopRequest) GetBorrowVault() optionalnullable.OptionalNullable[string] {
	if c == nil {
		return nil
	}
	return c.BorrowVault
}

func (c *CreditLoopRequest) GetSubAccountID() *int64 {
	if c == nil {
		return nil
	}
	return c.SubAccountID
}

func (c *CreditLoopRequest) GetCollateralToken() string {
	if c == nil {
		return ""
	}
	return c.CollateralToken
}

func (c *CreditLoopRequest) GetBorrowToken() string {
	if c == nil {
		return ""
	}
	return c.BorrowToken
}

func (c *CreditLoopRequest) GetInitialCollateralAmount() InitialCollateralAmount {
	if c == nil {
		return InitialCollateralAmount{}
	}
	return c.InitialCollateralAmount
}

func (c *CreditLoopRequest) GetMultiplier() Multiplier {
	if c == nil {
		return Multiplier{}
	}
	return c.Multiplier
}

func (c *CreditLoopRequest) GetLoanToValue() LoanToValue {
	if c == nil {
		return LoanToValue{}
	}
	return c.LoanToValue
}

func (c *CreditLoopRequest) GetMaxSlippagePercent() *CreditLoopRequestMaxSlippagePercent {
	if c == nil {
		return nil
	}
	return c.MaxSlippagePercent
}

func (c *CreditLoopRequest) GetEmodeCategory() optionalnullable.OptionalNullable[int64] {
	if c == nil {
		return nil
	}
	return c.EmodeCategory
}

func (c *CreditLoopRequest) GetGasSponsorship() *bool {
	if c == nil {
		return nil
	}
	return c.GasSponsorship
}

func (c *CreditLoopRequest) GetPreview() *bool {
	if c == nil {
		return nil
	}
	return c.Preview
}

func (c *CreditLoopRequest) GetPricing() *Pricing {
	if c == nil {
		return nil
	}
	return c.Pricing
}
