// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package components

import (
	"errors"
	"fmt"
	"github.com/CompassLabs/cli/internal/sdk/sdkinternal/utils"
	"github.com/CompassLabs/cli/internal/sdk/types"
)

type PriceLowerType string

const (
	PriceLowerTypeNumber PriceLowerType = "number"
	PriceLowerTypeStr    PriceLowerType = "str"
)

type PriceLower struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type PriceLowerType
}

func CreatePriceLowerNumber(number float64) PriceLower {
	typ := PriceLowerTypeNumber

	return PriceLower{
		Number: &number,
		Type:   typ,
	}
}

func CreatePriceLowerStr(str string) PriceLower {
	typ := PriceLowerTypeStr

	return PriceLower{
		Str:  &str,
		Type: typ,
	}
}

func (u *PriceLower) UnmarshalJSON(data []byte) (err error) {
	previous := *u
	*u = PriceLower{}
	defer func() {
		if err != nil {
			*u = previous
		}
	}()

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  PriceLowerTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  PriceLowerTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceLower", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceLower", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(PriceLowerType)
	switch best.Type {
	case PriceLowerTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case PriceLowerTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceLower", string(data))
}

func (u PriceLower) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type PriceLower: all fields are null")
}

type PriceUpperType string

const (
	PriceUpperTypeNumber PriceUpperType = "number"
	PriceUpperTypeStr    PriceUpperType = "str"
)

type PriceUpper struct {
	Number *float64 `queryParam:"inline" union:"member"`
	Str    *string  `queryParam:"inline" union:"member"`

	Type PriceUpperType
}

func CreatePriceUpperNumber(number float64) PriceUpper {
	typ := PriceUpperTypeNumber

	return PriceUpper{
		Number: &number,
		Type:   typ,
	}
}

func CreatePriceUpperStr(str string) PriceUpper {
	typ := PriceUpperTypeStr

	return PriceUpper{
		Str:  &str,
		Type: typ,
	}
}

func (u *PriceUpper) UnmarshalJSON(data []byte) (err error) {
	previous := *u
	*u = PriceUpper{}
	defer func() {
		if err != nil {
			*u = previous
		}
	}()

	var candidates []utils.UnionCandidate

	// Collect all valid candidates
	var number float64 = float64(0)
	if err := utils.UnmarshalJSON(data, &number, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  PriceUpperTypeNumber,
			Value: &number,
		})
	}

	var str string = ""
	if err := utils.UnmarshalJSON(data, &str, "", true, nil); err == nil {
		candidates = append(candidates, utils.UnionCandidate{
			Type:  PriceUpperTypeStr,
			Value: &str,
		})
	}

	if len(candidates) == 0 {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceUpper", string(data))
	}

	// Pick the best candidate using multi-stage filtering
	best := utils.PickBestUnionCandidate(candidates, data)
	if best == nil {
		return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceUpper", string(data))
	}

	// Set the union type and value based on the best candidate
	u.Type = best.Type.(PriceUpperType)
	switch best.Type {
	case PriceUpperTypeNumber:
		u.Number = best.Value.(*float64)
		return nil
	case PriceUpperTypeStr:
		u.Str = best.Value.(*string)
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for PriceUpper", string(data))
}

func (u PriceUpper) MarshalJSON() ([]byte, error) {
	if u.Number != nil {
		return utils.MarshalJSON(u.Number, "", true)
	}

	if u.Str != nil {
		return utils.MarshalJSON(u.Str, "", true)
	}

	return nil, errors.New("could not marshal union type PriceUpper: all fields are null")
}

// PriceRange - Bounds as prices (token1 per token0, decimals-adjusted).
type PriceRange struct {
	//lint:ignore U1000 accessed via reflection for JSON marshaling
	type_      *string    `const:"prices" json:"type,omitzero"`
	PriceLower PriceLower `json:"price_lower"`
	PriceUpper PriceUpper `json:"price_upper"`
}

func (p PriceRange) MarshalJSON() ([]byte, error) {
	return utils.MarshalJSON(p, "", false)
}

func (p *PriceRange) UnmarshalJSON(data []byte) error {
	if err := utils.UnmarshalJSON(data, &p, "", false, nil); err != nil {
		return err
	}
	return nil
}

func (p *PriceRange) GetType() *string {
	return types.Pointer("prices")
}

func (p *PriceRange) GetPriceLower() PriceLower {
	if p == nil {
		return PriceLower{}
	}
	return p.PriceLower
}

func (p *PriceRange) GetPriceUpper() PriceUpper {
	if p == nil {
		return PriceUpper{}
	}
	return p.PriceUpper
}
